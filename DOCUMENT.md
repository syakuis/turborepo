# document

## TurboRepo

### globalDependencies

- **globalDependencies**는 turbo.json에서 지정한 파일 패턴(예: `.env.*local`)이 변경될 때, Turborepo의 모든 작업 캐시가 무효화되어 전체 작업이 새로 실행됩니다.
- 주로 환경 변수 파일 등, 프로젝트 전체에 영향을 주는 파일이 바뀔 때 캐시 무효화를 위해 사용합니다.
- 이를 통해 코드 외부의 중요한 파일 변경도 빌드 결과에 즉시 반영되도록 보장합니다.

캐시 무효화란, 기존에 저장해둔 캐시 데이터를 더 이상 사용하지 않고 삭제하거나 새로 갱신하는 과정을 말합니다.  
이렇게 하는 이유는 코드, 설정, 의존성 등 중요한 값이 바뀌었을 때, 예전(오래된) 캐시를 계속 쓰면 잘못된 결과나 오류가 발생할 수 있기 때문입니다[1][4].  
따라서 캐시 무효화를 통해 항상 최신 상태의 데이터와 결과를 사용할 수 있도록 보장합니다.

### globalEnv

지정한 환경 변수의 값이 변경될 때 Turborepo의 모든 작업 캐시를 무효화하여, 빌드 결과가 항상 최신 환경을 반영하도록 보장하는 옵션입니다.


## Npm

### `auto-install-peers = true`

- `auto-install-peers = true`를 .npmrc에 설정하면, pnpm이 누락된 필수 peer dependency를 자동으로 설치합니다.
- 만약 여러 패키지가 서로 다른 버전의 같은 peer dependency를 요구하면, 자동 설치는 되지 않고 경고만 출력됩니다.
- 이 기능은 pnpm에서만 동작하며, npm은 v7부터 기본 지원, yarn은 자동 설치를 지원하지 않습니다.
